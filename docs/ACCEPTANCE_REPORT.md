# RelicScope AI Spark Demo 验收报告

验收日期：2026-08-30  
对应 OpenSpec 变更：`build-relicscope-dual-spark-demo`  
交付版本：`1.1.0`

## 1. 验收结论

本地软件、图片/视频结构化报告、单机降级流程和双 Spark 部署配置已完成。成熟框架复用与许可证治理任务已并入 OpenSpec。当前完成 **47/48** 项；只剩真实双机环境任务：

- `9.4`：两台 DGX Spark 私网端到端冒烟、节点身份、真实模型运行和无公网业务流量留证；
当前结果可用于图片、视频、复拍比较、证据图、主动检测与结构化报告的本地演示，以及 Spark 上机准备。它不代表双 Spark 实机已经验收，也不构成文物鉴定结论。

## 2. 自动化与静态验收

| 检查 | 结果 |
|---|---|
| Python 单元、集成、安全与边界测试 | 103 passed（2026-08-30复跑） |
| Python 静态检查 | 通过 |
| Python 应用及测试模块编译 | 通过 |
| 前端 JavaScript 语法 | 通过 |
| SVG XML 有效性 | 通过 |
| 全部部署脚本 Bash 语法 | 通过 |
| 双节点 Compose 配置解析 | 通过 |
| 单节点 Compose 配置解析 | 通过 |
| OpenSpec strict | 通过 |
| 真实媒体烟雾 | JPEG×2、复拍比较、MP4、10 帧、报告、完整性与重启持久性通过 |
| 规格—测试映射 | 既有 Requirement 与 v1.1 媒体兼容扩展均已映射；9.4 单列待实机 |

## 3. ARM64 容器证据边界

当前交付不附带此前原型 ARM64 镜像的镜像 ID、平台清单或运行日志，因此不把历史记录计为 `1.1.0` 验收证据。下表中的 `1.1.0` 镜像必须在目标 Spark 重新构建并补录实测值。

| 证据 | 1.1.0 状态 |
|---|---|
| 镜像 | 目标：`relicscope-ai-demo:1.1.0-arm64` |
| 镜像 ID | 待目标Spark构建后填写 |
| 平台 | 目标：`linux/arm64`；待复验 |
| 运行用户 | 目标：非root且与持久目录UID/GID一致；待复验 |
| 根文件系统 / capabilities | 目标：只读 / 全部移除；待复验 |
| 数据重启持久性 | 本机进程重启已通过；待以 1.1.0 ARM64 容器复演并记录会话 ID |
| 容器健康与模式 | 待记录`healthy`、`single-degraded`或`dual-node`实际响应 |
| 知识版本 | 待记录目标部署实际值 |

复验步骤：启动 1.1.0 容器，创建媒体会话，记录原始媒体与审计尾哈希，重启同一容器，再按原会话 ID 读取；同时验证 `/var/lib/relicscope` 可写、`/opt/relicscope` 不可写。完成前，不对外宣称 1.1.0 镜像已通过 ARM64 实机验收。

## 4. 最新浏览器全流程

最终代码在本机回环服务重新执行一键 P01：

- 会话：`RS-7B595CBA1D1D4D56`
- 报告：`RPT-C705A547C894478E`
- 报告包 SHA-256：`bbf279edeba2ac53b61e4526e6e720caf81d596b0e76ed4fe678c364cab1da95`
- 本地知识引用：3 条，均绑定版本与精确来源位置
- Raman：低信噪质量失败，物理实耗仍先行结算，命题不更新
- HSI：受约束重规划后质量通过，不确定度由 0.85 降至 0.48
- 审计链：9 个连续事件，包含两次原子执行权取得
- 输出：`REVIEW REQUIRED`，持续显示 `DEMO/SYNTHETIC` 与“非真实鉴定结论”

界面验收详情见 `docs/UI_ACCEPTANCE.md`。

## 5. 真实媒体闭环

`scripts/media-smoke.py` 对运行中的同一服务提交两张真实 JPEG、一个真实 MP4 及其 10 个时间戳帧，并生成结构化报告：

- 会话：`RS-1832B4B6D87E46BC`
- 图片质量：`PASS / PASS`
- 同区域复拍：`STABLE_WITHIN_CAPTURE_TOLERANCE`
- 视频：`VID-755370770DAC493E`
- 原视频 SHA-256：`f7d73ef49a29e92a1a08b846b2efefb066304c3c5acbbb8f0f3659cb70b9124b`
- 采样：10 帧；近重复抑制 8 帧；代表帧 2 帧
- 视频总门控：未通过，原因是测试素材只有轻微缩放，不能冒充充分多视角覆盖
- 报告：`RPT-FB407DB41B624594`
- 报告 SHA-256：`6925cf00cf5eacbbb9b51c5ebce46412fd29451d55faaa8c1de92c6c2834188b`
- 证据图：29 节点 / 43 边；Next Best Observation 3 项
- 重启持久性：重启服务后会话、报告和 13 个原始/派生文件全部复算有效

这一结果同时证明“质量先于模型”的拒绝路径：系统保留全部帧及来源，抑制近重复推理，并要求补拍，不把不足视频包装为可靠外观结论。

## 6. 双 Spark 上机门槛

完成 9.4 需要提供两台设备的固定私网 IP、SSH 用户和获授权的认证方式。上机后保存以下证据：

1. Spark A 视觉/嵌入服务与 Spark B 网关/证据服务的真实健康响应；
2. 实际模型 ID、容器服务版本、节点 ID、角色、请求与响应哈希；
3. 正确密钥成功、缺失或错误密钥拒绝且日志无密钥；
4. 经 Spark B 单一浏览器入口完成完整 P01 与报告；
5. 断公网运行记录、节点失联降级和恢复时间线；
6. 最终规格—测试矩阵复核及交付包哈希。

在这些实机证据齐备前，不应对外使用“双 Spark 实机已部署通过”的表述。
