## 1. 数据契约与导入

- [x] 1.1 定义参考器物、参考图、假货记录、许可、审核和发布版本 schema。
- [x] 1.2 实现路径边界、MIME、SHA-256、重复图片、稳定标识和最少视角校验。
- [x] 1.3 提供 50 件资料目录模板、清单示例和导入问题报告；所有示例显式标记为合成或占位。

## 2. 嵌入与索引

- [x] 2.1 实现本地 Qwen3-VL-Embedding-2B 服务与模型身份、revision、输入/输出哈希验证。
- [x] 2.2 构建不可变图像向量索引并绑定参考库版本、图片哈希和模型版本。
- [x] 2.3 实现精确余弦后端与可选 cuVS 适配边界，验证两者在测试向量上的 Top-K 一致性。

## 3. 识别与反证

- [x] 3.1 实现多视角聚合、绝对阈值、Top-1/Top-2 间隔、质量和覆盖门槛。
- [x] 3.2 实现库内候选、相关参考、开放集拒识、采集不足、待校准和模型不可用状态。
- [x] 3.3 实现独立假货空间检索、审核等级加权和反证信号，验证其不会直接产生真伪裁决。

## 4. API、证据与界面

- [x] 4.1 提供库状态、索引版本和会话识别 API，并将运行绑定到已上传原图。
- [x] 4.2 将识别结果、模型运行和参考/反证引用写入审计链、证据图和报告。
- [x] 4.3 在界面分开展示“同件候选”“相关参考”“反证信号”“拒识原因”和“建议补拍”。

## 5. 校准与验收

- [x] 5.1 实现独立复拍/库外负例校准工具，禁止用建库原图自匹配生成校准。
- [x] 5.2 输出 Top-1、Top-5、FAR、FRR、每视角召回、模型/数据哈希和推荐阈值。
- [ ] 5.3 在目标 Spark 完成至少三类冻结场景，并由专家审核错误案例后发布 demo-v1。
