## 1. 基线与运行骨架

- [x] 1.1 盘点并补齐现有 Python 原型模块，运行编译检查并记录任何缺失入口或循环依赖，验证全部应用模块能够被 Python 导入。
- [x] 1.2 固定应用与测试依赖、运行目录和环境配置，验证在干净虚拟环境中能够完成依赖安装且默认不需要公网服务运行。
- [x] 1.3 建立隔离数据库、临时上传目录和模型替身测试夹具，验证测试执行不会读写生产数据目录或调用外部端点。

## 2. 器物会话与统一 API

- [x] 2.1 完成器物、声明、区域、原始文件和会话版本的数据状态，验证有效更新使版本单调递增而无效请求不改变状态。
- [x] 2.2 强化文件接收、MIME/尺寸/图像解码和安全文件名校验，验证损坏文件与不存在区域被拒绝且不登记为证据输入。
- [x] 2.3 完成会话编排服务，统一连接图像、知识、规划、执行、证据和报告状态，验证非法状态转换返回明确错误且不产生部分更新。
- [x] 2.4 建立 FastAPI 应用入口及会话创建、查询和健康接口，使用 API 测试验证响应结构、错误码、协议版本和 `DEMO/SYNTHETIC` 状态。

## 3. 本地知识库与可引用检索

- [x] 3.1 定义知识清单、条目、分块、审核状态、许可边界和不可变版本格式，使用有效与缺字段样例验证入库规则。
- [x] 3.2 建立小型演示知识库及内容哈希，验证所有内置条目都标记 `DEMO/SYNTHETIC`、具有适用范围并能按版本重新装载。
- [x] 3.3 实现本地全文索引、向量索引和可替换嵌入接口，验证相同版本与相同查询可返回可复核的候选集合。
- [x] 3.4 实现模型/嵌入服务不可用时的确定性降级，断开测试端点后验证系统显示 degraded、只访问本地数据且不静默调用公网。
- [x] 3.5 实现知识空间、访问范围、最低分数和引用白名单策略，验证低相关候选、越权条目和模型虚构引用不能进入证据链。
- [x] 3.6 提供知识检索 API 并把查询哈希、知识版本、算法版本与实际引用写入会话审计，验证返回结果含分数、匹配依据、片段和精确来源位置。

## 4. 多模态感知与模型追溯

- [x] 4.1 完成 RGB 图像质量指标、候选区域和确定性视觉指纹，使用相同字节复算测试验证指纹稳定且质量失败项可观察。
- [x] 4.2 完成受限 VLM 观察客户端和结构化输出校验，使用违规模型响应测试验证真伪、确定年代、价格与法律结论被拒绝接纳。
- [x] 4.3 保存成功与失败模型运行的节点、角色、模型、任务模板哈希、输入/输出引用、耗时和降级状态，验证审计查询可区分真实调用与确定性回退。
- [x] 4.4 将图像、指纹、候选区域、VLM 观察与知识召回写入证据图，验证每个派生节点都能回溯到原始文件、区域和运行记录。

## 5. P01 主动科学检测闭环

- [x] 5.1 完成安全硬约束、预期信息增益、成本和风险效用计算，使用超预算 XRF 与低价值动作测试验证它们不能被选中。
- [x] 5.2 完成唯一动作标识、幂等提交和事务内风险预算原子预留，使用并发测试验证余额只允许一个竞争动作成功且不出现部分预留。
- [x] 5.3 完成仪器适配器契约与 Raman、HSI、UV 演示回放，验证所有回放输出带 `DEMO/SYNTHETIC`、设备状态和风险遥测字段。
- [x] 5.4 完成实际/保守暴露结算并确保其先于质量门控，使用失败和遥测缺失测试验证物理风险不会因数据无效而退回。
- [x] 5.5 完成质量门控、失败指纹抑制、重规划、停止与弃权状态，验证不合格数据保持命题不变且同参数动作不能被立即重选。
- [x] 5.6 建立固定 P01 场景测试：Raman 低信噪失败并扣除实耗、XRF 超预算阻断、HSI 通过后不确定度从 0.85 降至 0.48，验证事件顺序与预期值一致。

## 6. 证据图、审计链与报告

- [x] 6.1 完成受控证据关系和数据等级，使用支持/冲突/质量失败组合测试验证失败观测不改变命题且冲突证据不会被隐藏。
- [x] 6.2 完成规范化 JSON 哈希事件链和验证接口，篡改测试验证载荷、顺序或前序哈希变化均会导致校验失败。
- [x] 6.3 完成确定性结构化报告模型，验证报告包含会话/协议版本、文件哈希、知识引用、模型运行、风险账本、证据图、结论边界、局限和最新审计哈希。
- [x] 6.4 接入可选本地推理摘要并执行输出限界校验，模拟推理端点故障与违规文本，验证报告仍可生成且不出现被禁止结论。
- [x] 6.5 提供 JSON 与人类可读报告下载并计算报告包哈希，验证同一冻结会话版本可复核且会话更新后生成新报告版本。

## 7. 一键演示界面

- [x] 7.1 构建无外部 CDN 的响应式控制台，验证首屏同时显示双节点/单机状态、当前模式、模型身份、知识版本和显著演示免责声明。
- [x] 7.2 实现建档、图像上传、质量/指纹和本地知识引用视图，验证界面展示的数据均来自后端 API 且可定位来源。
- [x] 7.3 实现候选动作比较、风险预算、执行时间线和一键 P01 流程，验证界面真实调用规划与执行 API 并展示 Raman 失败、风险扣除和 HSI 重规划。
- [x] 7.4 实现证据图、审计链验证、保守结论和报告下载视图，验证 `DEMO/SYNTHETIC` 与“非真实鉴定结论”在首屏和报告区均默认可见。
- [x] 7.5 执行桌面与窄屏浏览器检查，验证主要操作可键盘访问、长哈希可读、加载/错误/降级状态不会遮蔽科学边界。

## 8. 双 Spark 与单机部署

- [x] 8.1 提供固定版本的 ARM64 容器定义、非 root 用户、持久卷和存活/就绪检查，并以静态检查和本机进程重启验证数据持久化契约；目标 ARM64 容器构建与重启实测归入 9.4。
- [x] 8.2 配置 Spark A 的视觉模型、嵌入/检索计算和受认证私网端点，验证健康响应的数据结构能够承载节点、模型、服务版本和就绪状态；真实节点与模型值归入 9.4。
- [x] 8.3 配置 Spark B 的网关、知识与证据存储及可选推理模型，验证本机降级模式下浏览器只需访问单一入口即可完成流程；真实跨节点路径归入 9.4。
- [x] 8.4 加入节点间 API key、私网地址校验、密钥注入和日志脱敏，使用无密钥及非允许地址测试验证请求被拒绝且密钥不出现在日志。
- [x] 8.5 提供角色可交换和单台 Spark 降级配置，验证调整环境配置无需改业务代码，且界面不会把单机或缓存回退显示为双机 AI。
- [x] 8.6 提供模型/镜像预缓存、离线启动、预检和回滚脚本，并以语法、配置及策略检查验证缺失依赖会被明确报告；隔离网络中的无公网流量实测归入 9.4。

## 9. 验收、部署实测与交付

- [x] 9.1 运行单元测试覆盖文件完整性、图像指纹、知识版本、风险预算、质量门控、证据关系、审计哈希和报告边界，验证测试套件全部通过。
- [x] 9.2 运行 API 集成测试覆盖建档至报告的完整状态机、非法顺序、模型降级和知识引用，验证无未处理异常或静默部分写入。
- [x] 9.3 运行安全与内容边界测试，验证超限上传、路径输入、虚构引用、违规模型结论、外部端点和凭据泄露均被阻断或显著告警。
- [ ] 9.4 在获得两台 Spark 私网地址和操作权限后运行双节点预检与端到端冒烟测试，验证运行记录显示 A/B 实际执行位置、服务版本、请求/响应哈希和无公网业务流量。
- [x] 9.5 编写中文 README、一天执行时间表、5 分钟演示台词、真实仪器适配说明和已知限制，按文档在干净环境复演并记录所需时间。
- [x] 9.6 生成验收报告包和规格—测试映射，运行 `openspec validate --strict`、全部测试及报告哈希校验，确认除 9.4 明确保留的目标硬件事项外，其余能力均有可复核证据后交付。

## 10. 成熟框架复用与第三方边界

- [x] 10.1 调研 NVIDIA DGX Spark playbooks、vLLM、NIM、NeMo Retriever、Holoscan、DeepStream 与 cuVS/Milvus，记录 `DEPLOYMENT_READY`、`EVALUATION` 和 `PHASE_2` 状态，验证运行健康信息不会把未接入组件标为可用。
- [x] 10.2 调研 RamanSPy、PyMca、Spectral Python、Open3D、OpenLIME、HyperSpy 与 anomalib，定义统一科学结果信封及启用门槛，验证异常检测不会被表述为真伪结论。
- [x] 10.3 建立第三方许可证和数据禁区，验证发布包不包含 MVTec AD 非商业数据、许可证不清的 CE5-DET、未批准模型权重或第三方研究数据。
