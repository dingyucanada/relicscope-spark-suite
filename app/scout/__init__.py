"""Scout V2 capture gateway and durable local inference workflow.

Package import stays side-effect free so the host-side provisioning utility can run with
the DGX OS Python standard library. Runtime components use their concrete modules.
"""
